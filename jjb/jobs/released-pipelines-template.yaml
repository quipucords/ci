---
- job-template:
    name: 'qpc-{release}-{install_type}-install-pipeline'
    project-type: pipeline
    sandbox: true
    triggers:
      - timed: '@midnight'
    dsl: !include-raw: 'pipelines/qpc-release-test-install.groovy'
